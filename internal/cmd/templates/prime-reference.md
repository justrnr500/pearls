## Quick Reference

- `pl search "query" --semantic` — Find relevant knowledge
- `pl context --for <path>` — Get context for a file area
- `pl create <id> --type <type>` — Save new knowledge
- `pl list` — See all pearls
